【整体布局】
- 页面背景：#F7F9FC，卡片背景白色，圆角16px，轻阴影。
- 最大宽度1400px，居中，左右内边距24px。
- 顶部一行放：用户头像（邮箱缩略）+ 刷新/设置按钮，改为悬浮显示邮箱全称。

【信息仪表盘区】（取代原顶部混乱区域）
- 用横向卡片或Flex网格展示以下数据，每个数据有标题和数值：
  - 市场情绪：显示“偏空 ▼ -8%”，用环形进度条或简洁箭头。
  - 今日事件数：4
  - 利好：1
  - 利空：2
  - 涉及个股：12（可标注较昨日变化）
  - 本周累计：11
- 右侧放置“事件影响雷达”图（可先做占位示意，用SVG模拟或Chart.js占位，要求视觉简洁，鼠标悬浮显示示例数据）。

【事件列表区】
- 分为两个选项卡：“正在影响”（默认激活，显示11条）和“今日已影响”（显示0条），带计数徽章。
- 每条事件采用紧凑卡片设计，包含：
  - 第一行：股票名称+代码（如“沃格光电 603773”）| 标签（利好/利空/中性，颜色：绿/红/橙）| 相对时间（如“10分钟前”）
  - 第二行：事件标题（加粗）+ 简短摘要（自动截断两行）
  - 第三行：AI判断小圆点 + 文本（例如“🟢 可能推动上涨”或“🔴 关注下行风险”）
  - 右侧放置一个“深度分析”按钮和一个路径预览图标 ⓘ（点击或悬浮时用弹窗或toast提示显示“AI影响路径”简要内容，此处用alert占位即可）。
- 移除原界面中重复的“关注利好/利空/中性”文字，以及独立的“关联个股”行和“AI影响路径”按钮。
- 每个卡片之间间距16px，桌面宽屏下采用双列瀑布流布局（grid）。

【字体与颜色】
- 数字/标题用Inter或系统默认无衬线字体。
- 主数值24px半粗，次要文字13px常规，对比度符合WCAG标准。
- 利好标签：#00A86B；利空标签：#E74C3C；中性：#F39C12。
- 强调按钮颜色：#2A6DFF。

【交互动效】
- 卡片悬浮时轻微上浮（translateY -2px），阴影加深。
- 选项卡切换有下划线滑动效果。
- 雷达图区域可以简单用CSS渐变圆环 + 文字占位，表示未来可接入真实数据。

【去除冗余】
- 删除所有“关注xx”前缀、“关联个股”重复信息。
- 时间显示超过24小时的改用“5月15日”格式，悬浮显示完整时间（可暂用固定示例数据实现）。

【示例数据】
使用原界面中的事件数据（沃格光电、海泰新光、ST张家界、中信证券、对位芳纶、中钨高新、华电辽能、山东赫达、恩捷股份、斯迪克、众合科技等），按照上述结构重写。

请输出完整的HTML/CSS/JS代码（单文件），包含必要的响应式适配（移动端改为单列），并确保在暗色模式下不影响可读性（可选，但如果能提供简单的暗色模式切换按钮更佳）。