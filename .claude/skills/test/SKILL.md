---
name: test
description: 基于需求或代码生成结构化的场景测试用例（Specs, cURL, SQL, Python）。
---

# Scenario Test (场景测试生成)

⚠️ **CRITICAL**: 执行此技能时，MUST 先执行初始化检查，禁止直接开始分析或生成测试。

**⚠️ 第一步必须执行**: 无论用户消息中是否包含输入，都必须先输出"初始化检查"部分的模板，等待用户提供测试目标和确认测试范围后，才能开始执行后续步骤。

此技能分析业务上下文，识别各类测试场景，并生成可执行的测试脚本。

> **交互协议**: 本指令严格遵循 `jl-skills/instructions/INTERACTION_PROTOCOL.md` 中定义的交互规范。

---

## ⚠️ 关键行为约束 (CRITICAL BEHAVIOR CONSTRAINTS)

> **这些约束是强制性的，违反将导致流程失败。**

### 约束 0: 初始化检查规则 ⚠️ CRITICAL

```
🛑 STOP RULE: 必须先询问输入

执行任何步骤前，MUST 先检查用户是否提供了必要的输入：
- 有输入 → 确认输入后询问测试范围，然后开始执行
- 无输入 → 必须先询问，禁止直接开始执行

⚠️ 禁止行为：
- ❌ 禁止直接开始分析代码或生成测试
- ❌ 禁止跳过初始化检查
- ❌ 禁止假设用户意图

✅ 必须行为：
- ✅ 必须先输出初始化检查模板
- ✅ 必须等待用户提供输入
- ✅ 必须等待用户确认测试范围
```

### 约束 1: 单步输出规则

```
🛑 ONE STEP AT A TIME

- 每次回复只输出一个步骤的内容
- 每个步骤输出后必须停止，等待用户确认
- 禁止在一次回复中包含多个步骤的内容
- 用户回复"确认/继续/OK"后才能输出下一步
```

### 约束 2: 对话框输出 vs 文件写入

```
📤 对话框输出 (每个步骤):
- 进度条和看板表格
- 场景概览表
- Mermaid 图表（时序图等）
- cURL 和 SQL 脚本（预览）
- 确认问题

📁 文件写入 (阶段结束时):
- 步骤4: 报告写入（包含 cURL 和 SQL 脚本）
- 步骤5: Python 测试脚本（独立文件）
```

---

## 能力 (Capabilities)

- **测试范围确认**: 明确需要补充哪部分测试
- **场景概览**: 生成测试场景概览表
- **流程介绍**: 介绍业务逻辑与流程（含Mermaid图）
- **脚本生成**: 生成 cURL 和 SQL 准备脚本（保留在报告中）
- **报告生成**: 生成完整的测试报告（包含 cURL 和 SQL 脚本）
- **端到端测试**: 生成 Python 端到端测试脚本（独立文件）

---

## 初始化检查 ⚠️ CRITICAL

> **⚠️ 强制要求**: 无论用户消息中是否包含输入，都必须先执行此初始化检查，禁止直接开始分析或生成测试。

### 检查 1: 测试目标

**⚠️ 执行规则（强制）**:
1. **第一步**: 必须先输出下面的"输出模板"，禁止跳过
2. **第二步**: 等待用户提供输入
3. **第三步**: 用户提供输入后，确认输入并询问测试范围

**禁止行为**:
- ❌ 禁止直接开始分析代码
- ❌ 禁止直接开始生成测试场景
- ❌ 禁止跳过初始化检查
- ❌ 禁止假设用户意图

**必须行为**:
- ✅ 必须先输出下面的模板
- ✅ 必须等待用户回复
- ✅ 必须等待用户确认测试范围

**输出模板（必须输出）**:

```markdown
## 开始生成测试用例

我已准备好生成场景测试用例。

**整体流程**:
- 步骤1: 测试范围确认 - 确认补充哪部分测试
- 步骤2: 场景概览 - 生成测试场景概览表
- 步骤3: 流程介绍 - 介绍业务逻辑与流程
- 步骤4: 脚本生成 - 生成 cURL 和 SQL 准备脚本（在对话框显示，最终写入报告）
- 步骤5: 报告写入 - 写入完整的测试报告（包含 cURL 和 SQL 脚本）
- 步骤6: Python 测试脚本 - 生成端到端测试脚本（独立文件）

---

🛑 **需要您的输入**

请提供以下信息之一：
1. 需求文档或设计文档路径
2. 选中要测试的代码/接口
3. API 接口定义（Swagger/OpenAPI）

**请问您希望为什么生成测试？**
```

**🛑 STOP - 等待用户提供输入**

⚠️ **重要**: 
- 用户未提供输入前，禁止执行任何后续步骤
- 禁止直接开始分析代码或生成测试
- 必须等待用户明确回复

---

### 检查 2: 测试范围确认

**前置条件**: 
- ✅ 用户已提供测试目标输入
- ✅ 已输出检查1的模板并等待用户回复

**⚠️ 执行规则（强制）**:
1. **第一步**: 确认用户提供的测试目标
2. **第二步**: 输出下面的测试范围确认模板
3. **第三步**: 等待用户确认测试范围

用户提供输入后，询问测试范围：

```markdown
---

🛑 **测试范围确认**

请选择需要补充的测试部分：
- **全部**（默认）- 正常流程、异常流程、边界测试、并发测试
- **正常流程** - 只生成正常流程测试
- **异常流程** - 只生成异常流程测试
- **边界测试** - 只生成边界测试
- **并发测试** - 只生成并发测试
- **自定义** - 请说明具体需要哪些场景

**请选择测试范围：**
```

**🛑 STOP - 等待用户确认**

⚠️ **重要**: 
- 用户未确认测试范围前，禁止执行任何后续步骤
- 必须等待用户明确回复

---

## 执行流程

⚠️ **前置条件检查**: 
在执行任何步骤之前，MUST 先完成以下检查：
- ✅ 已输出检查1的模板（测试目标询问）
- ✅ 用户已提供测试目标
- ✅ 已输出检查2的模板（测试范围确认）
- ✅ 用户已确认测试范围

**如果以上条件未满足，禁止执行后续步骤，必须先完成初始化检查。**

### 步骤 1: 场景概览

**加载**: `jl-skills/instructions/test/scenario-overview-instructions.md`

**⚠️ 执行规则（强制）**:
1. **只加载并执行步骤 1.1**（识别测试场景并生成概览表）
2. **输出步骤 1.1 的内容后，立即停止**
3. **等待用户确认后**，才能继续执行步骤 1.2（如果有）
4. **禁止一次性输出多个步骤的内容**
5. **禁止跳过用户确认**

**输出**: 按照模板格式生成场景概览表（只输出步骤 1.1 的内容）

**🛑 STOP HERE - 必须等待用户确认后才能继续**

⚠️ **重要**: 
- 用户未回复"确认"前，禁止执行任何后续步骤
- 禁止输出步骤2的内容
- 禁止输出步骤 1.2 的内容（如果有）

**[等待用户确认]**

---

### 步骤 2: 流程介绍

**前置条件**: 用户已确认步骤1

**加载**: `jl-skills/instructions/test/flow-introduction-instructions.md`

**⚠️ 执行规则（强制）**:
1. **只加载并执行步骤 2.1**（业务逻辑分析与流程介绍）
2. **输出步骤 2.1 的内容后，立即停止**
3. **等待用户确认后**，才能继续执行步骤 2.2（如果有）
4. **禁止一次性输出多个步骤的内容**
5. **禁止跳过用户确认**

**输出**: 介绍业务逻辑与流程，包含 Mermaid 时序图（只输出步骤 2.1 的内容）

**🛑 STOP HERE - 必须等待用户确认后才能继续**

⚠️ **重要**: 
- 用户未回复"确认"前，禁止执行任何后续步骤
- 禁止输出步骤3的内容
- 禁止输出步骤 2.2 的内容（如果有）

**[等待用户确认]**

---

### 步骤 3: 脚本生成（cURL 和 SQL）

**前置条件**: 用户已确认步骤2

**加载**: `jl-skills/instructions/test/script-generation-instructions.md`

**⚠️ 执行规则（强制）**:
1. **只加载并执行步骤 3.1**（脚本生成说明）
2. **输出步骤 3.1 的内容后，立即停止**
3. **等待用户确认后**，才能继续执行步骤 3.2（生成脚本内容）
4. **禁止一次性输出步骤 3.1 和 3.2**
5. **禁止跳过用户确认**
6. cURL 和 SQL 脚本**生成后保留在内存中，等待步骤4写入报告**
7. 按照概览列出的全部场景一个一个实现
8. 在对话框中显示脚本内容预览（便于审查）

**输出**: 
- 步骤 3.1: 脚本生成说明
- 步骤 3.2: 脚本内容预览（等待用户确认步骤 3.1 后）

**🛑 STOP HERE - 必须等待用户确认后才能继续**

⚠️ **重要**: 
- 用户未回复"确认"前，禁止执行任何后续步骤
- 禁止输出步骤4的内容
- 禁止输出步骤 3.2 的内容（直到用户确认步骤 3.1）

**[等待用户确认]**

---

### 步骤 4: 报告写入

**前置条件**: 用户已确认步骤3

**加载**: `jl-skills/instructions/test/report-writing-instructions.md`

**⚠️ 执行规则（强制）**:
1. **只加载并执行步骤 4.1**（报告整合准备）
2. **输出步骤 4.1 的内容后，立即停止**
3. **等待用户确认后**，才能继续执行步骤 4.2（报告写入）
4. **禁止一次性输出步骤 4.1 和 4.2**
5. **禁止跳过用户确认**
6. 使用模板 `jl-skills/templates/JL-Template-Scenario-Test-Case.md`
7. 整合步骤1-3的所有内容
8. 写入报告文件

**输出**: 
- 步骤 4.1: 报告写入说明
- 步骤 4.2: 报告写入完成提示（等待用户确认步骤 4.1 后）

**🛑 STOP HERE - 必须等待用户确认后才能继续**

⚠️ **重要**: 
- 用户未回复"确认"前，禁止执行任何后续步骤
- 禁止输出步骤5的内容
- 禁止输出步骤 4.2 的内容（直到用户确认步骤 4.1）

**[等待用户确认]**

---

### 步骤 5: Python 端到端测试脚本

**前置条件**: 用户已确认步骤4

**加载**: `jl-skills/instructions/test/python-e2e-instructions.md`

**⚠️ 执行规则（强制）**:
1. **只加载并执行步骤 5.1**（Python 脚本生成准备）
2. **输出步骤 5.1 的内容后，立即停止**
3. **等待用户确认后**，才能继续执行步骤 5.2（生成脚本并写入文件）
4. **禁止一次性输出步骤 5.1 和 5.2**
5. **禁止跳过用户确认**
6. 新建一个 Python 文件
7. 写入端到端测试脚本
8. 包含所有场景的测试用例

**输出**: 
- 步骤 5.1: Python 脚本生成说明
- 步骤 5.2: 文件写入完成提示（等待用户确认步骤 5.1 后）

**🛑 STOP HERE - 必须等待用户确认后才能继续**

⚠️ **重要**: 
- 用户未回复"确认"前，禁止执行任何后续步骤
- 禁止输出步骤 5.2 的内容（直到用户确认步骤 5.1）

**[等待用户确认]**

---

## 测试完成: 自动写入总结

**触发条件**: 用户确认步骤5后，**立即执行**：

### 输出完成总结

```markdown
---

## ✅ 测试用例生成完成

| ✅ 已完成 |
|:----------|
| 1. 测试范围确认 |
| 2. 场景概览 |
| 3. 流程介绍 |
| 4. 脚本生成（cURL + SQL） |
| 5. 报告写入 |
| 6. Python 端到端测试脚本 |

### 📄 已写入文件

**报告文件**: `jl-skills/generated/test/{date}/Scenario_Test_Case.md`

**文件**:
- `jl-skills/generated/test/{date}/Scenario_Test_Case.md`（包含 cURL 和 SQL 脚本）
- `jl-skills/generated/test/{date}/scripts/e2e_test.py`（Python 端到端测试脚本）

### 统计
- P0 正常流程: X 个
- P1 异常/边界: X 个
- P2 并发: X 个
- **总计**: X 个场景

---

### 🗂️ 归档建议

**后续操作**: 运行 `/docs` 指令将本次测试结果归档为 ADR，并更新文档体系。

**归档内容**:
- ADR 记录: 测试范围、场景统计、覆盖情况
- 文档更新: `docs/QA.md`（测试索引）和 `docs/FEATURES/{module}/QA.md`（测试用例）
```