# AI Stock 项目宪章 (Constitution)

本宪章定义了项目的核心原则、技术约束和治理规则，回答"**能不能做**"的问题。

---

## 核心原则

### I. 三模块协同优先 (Three-Modules Synergy)

产品由 **模块一（AI 事件分析 & 知识库）**、**模块二（行情数据展示）**、**模块三（策略监控）** 构成，功能与数据须能互相联动。

- 新增能力必须标明归属模块，并说明与其它模块的**跳转或数据关联**（例如：分析中的股票代码 → K 线；自选股 → 监控信号；信号/个股 → 历史分析回溯）
- MVP 范围以 `docs/product-overview.md` 中的**核心跳转路径**与**阶段交付**为准，不做该产品边界中明确排除的能力（自动交易、Level 2、多市场等）

### II. 个人投研工具边界 (Research-Only)

本产品是**辅助研究工具**，不是券商、投顾或自动交易系统。

- **禁止**自动下单、实盘交易接口、代客理财类能力
- 行情与信号须体现**数据源延迟**与**算法简化**（如 AKShare、缠论程序化实现的局限）
- 涉及预测、信号、买卖点等表述时，须保留**不构成投资建议**语义，不承诺收益

### III. 简洁实用 (KISS & YAGNI)

- 代码修改遵循最小变更原则，不引入不必要的重构
- 不为假设的未来需求提前设计，按当前业务需求实现
- 三行相似代码优于一个过早的抽象
- 复用 Ant Design、既有服务封装与领域抽象，避免重复造轮子

---

## 技术约束

### 技术栈

- **前端**：React 18 + TypeScript + Ant Design 5 + ECharts 5 + **Zustand**
- **后端**：Python 3 + **FastAPI**
- **数据**：**MySQL**（主库）+ **Redis**（缓存/计算结果）；A 股数据 **AKShare**
- **大模型**：经统一抽象层接入（如 OpenAI / DeepSeek 兼容接口），**禁止**在业务层散落直连 SDK

> 持久化访问须经 **Repository**，主库以本宪章为准采用 **MySQL**；若 `rules/backend.md` 与宪章冲突，以本宪章为准。

### 架构红线

- **禁止跨层调用**：后端须遵守 Router → Application → Domain → Infrastructure；Router **禁止**直接访问数据库或调用大模型；Domain **禁止**依赖 FastAPI、**禁止**直接访问数据库与 AI
- **禁止前端绕开 Service**：页面与组件 **禁止**直接使用 `fetch`；所有 HTTP 调用集中在 `services/`
- **禁止泄露密钥**：API Key、数据库密码等仅来自环境变量（如 `.env`），**禁止**写入源码或提交仓库
- **流式输出须成对实现**：后端 SSE 格式须符合约定（`data: ...\n\n`）；前端须具备实时输出、滚动、停止与 loading（见 `rules/`）

### 数据约束

- 关键金额与比率在 Python 侧使用 **`Decimal`** 等合适类型，**禁止**用 `float` 承载核心业务数值
- 所有数据库操作在 **Repository** 内完成；JSON 可用于扩展字段，核心字段须可查询、可迁移
- Prompt 与提示词 **模板化管理**，禁止多处硬编码副本

---

## 治理

### 变更流程

1. **理解上下文**：修改前阅读相关 PRD（`docs/ai-analysis-prd.md`、`docs/market-data-prd.md`、`docs/strategy-monitor-prd.md`）及对应代码
2. **对齐分层**：前后端分别遵守 `rules/backend.md`、`rules/frontend.md`
3. **本地验证**：按仓库约定执行构建、类型检查与关键路径验证（前后端脚本以项目落地后的 `README.md` 为准）

### Code Review 清单

- [ ] 是否违反「个人投研工具」或模块边界？
- [ ] 后端是否出现 Router/Domain 直连 DB 或 AI？
- [ ] 前端是否出现页面/组件直连 `fetch`？
- [ ] 流式/SSE 与前端流式体验是否一致？
- [ ] 日志与配置是否避免泄露密钥与敏感请求体？

### 优先级

- 本宪章优先级高于个人编码习惯，所有代码变更必须符合宪章规定
- 宪章修改需记录修改原因和影响范围
- **详细执行规则见**：[rules/](./rules/) 目录

---

**版本**: 1.0.0 | **批准日期**: 2026-04-16 | **最后修订**: 2026-04-16
